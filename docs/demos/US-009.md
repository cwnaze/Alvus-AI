# US-009 — Verify rollback path (Worker + migration)

_Generated by this story's proving commands · 2026-08-08 · PASS_

## 1. A deliberately-broken deploy is rolled back via wrangler rollback [deployment-id], and the command output demonstrates the previous version serving again (exercised against a disposable scratch Worker, never the real alvus-ai production Worker, so this proof can't cause a real outage)

```console
$ ./scripts/demo-us-009-rollback-worker.sh
=== 1. Deploying the good version to a scratch Worker ===

 ⛅️ wrangler 4.119.0 (update available 4.120.0)
───────────────────────────────────────────────
Total Upload: 0.20 KiB / gzip: 0.17 KiB
Uploaded alvus-ai-rollback-demo (0.70 sec)
Deployed alvus-ai-rollback-demo triggers (0.49 sec)
  https://alvus-ai-rollback-demo.cwnaze-ms.workers.dev
Current Version ID: c573e44e-ec84-4965-8bc0-0826ad18067a

=== 2. Confirming the good version serves 200 ===
https://alvus-ai-rollback-demo.cwnaze-ms.workers.dev -> HTTP 200 (3 consecutive requests)

=== 3. Recording the good deployment's Version ID ===
Good Version ID: c573e44e-ec84-4965-8bc0-0826ad18067a

=== 4. Deploying a deliberately-broken version ===

 ⛅️ wrangler 4.119.0 (update available 4.120.0)
───────────────────────────────────────────────
… 27 lines omitted …
│ However, your local development environment will not be affected by this rollback.
│ Rolling back to a previous deployment will not rollback any of the bound resources (Durable Object, D1, R2, KV, etc).
│
│ (100%) c573e44e-ec84-4965-8bc0-0826ad18067a
│       Created:  2026-08-08T04:51:00.533266Z
│           Tag:  -
│       Message:  -
│
? Are you sure you want to deploy this Worker Version to 100% of traffic?
🤖 Using fallback value in non-interactive context: yes
Performing rollback...
│
╰  SUCCESS  Worker Version c573e44e-ec84-4965-8bc0-0826ad18067a has been deployed to 100% of traffic.

Current Version ID: c573e44e-ec84-4965-8bc0-0826ad18067a

=== 7. Confirming the previous version is serving again ===
https://alvus-ai-rollback-demo.cwnaze-ms.workers.dev -> HTTP 200 (3 consecutive requests)

PASS: wrangler rollback restored the previous working version.
```

## 2. A hand-written down-migration exists for the one applied migration (drizzle/migrations/0000_simple_blockbuster.sql)

```console
$ cat drizzle/rollback/0000_simple_blockbuster_down.sql
-- Hand-written down-migration for drizzle/migrations/0000_simple_blockbuster.sql.
--
-- Drizzle Kit only generates forward migrations (see docs/infra.md's Rollback
-- plan) -- this file is a manual, break-glass step for the emergency case
-- where a bad forward migration must be undone against a live database. It is
-- NOT tracked in drizzle/migrations/meta/_journal.json and `drizzle-kit
-- migrate` will never apply it. Run it by hand against DATABASE_URL:
--
--   psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f drizzle/rollback/0000_simple_blockbuster_down.sql
--
-- Drops in FK-safe order (dependents before the tables they reference).
-- Destructive: this deletes all rows in these tables, not just the schema.
DROP TABLE IF EXISTS "waitlist_signups";
--> statement-breakpoint
DROP TABLE IF EXISTS "tier_limits";
--> statement-breakpoint
DROP TABLE IF EXISTS "users";
```

## 3. The down-migration runs cleanly against the real DATABASE_URL (wrapped in BEGIN/ROLLBACK so this proof does not actually drop the tables)

```console
$ psql "${DATABASE_URL%%\?*}" -v ON_ERROR_STOP=1 -c "BEGIN" -f drizzle/rollback/0000_simple_blockbuster_down.sql -c "ROLLBACK"
BEGIN
DROP TABLE
DROP TABLE
DROP TABLE
ROLLBACK
```

## 4. Rollback procedure is documented in the README

```console
$ grep -n -A22 '^### Rollback' README.md
49:### Rollback
50-
51-Full policy (additive-first migration strategy, backup priorities) lives in
52-`docs/infra.md`'s Rollback plan. The two concrete emergency procedures:
53-
54-**Worker** — near-instant, the primary safety net for a bad code deploy:
55-```bash
56-npx wrangler deployments list        # find the last good Version ID
57-npx wrangler rollback [version-id]   # re-promote it to 100% of traffic
58-```
59-
60-**Schema** — Drizzle only generates forward migrations, so undoing one is a manual,
61-break-glass step. Hand-written down-migrations live in `drizzle/rollback/`, named after
62-the forward migration they undo (e.g. `drizzle/rollback/0000_simple_blockbuster_down.sql`
63-undoes `drizzle/migrations/0000_simple_blockbuster.sql`), and are never applied by
64-`drizzle-kit migrate` — only new destructive forward migrations get a matching one. Apply
65-by hand against `DATABASE_URL`:
66-```bash
67-# psql's URI parser rejects the ?pgbouncer=true suffix Supabase's pooled
68-# connection string carries -- strip the query string before connecting.
69-psql "${DATABASE_URL%%\?*}" -v ON_ERROR_STOP=1 -f drizzle/rollback/<name>_down.sql
70-```
71-This drops tables and their data — default to additive/backward-compatible forward
```
