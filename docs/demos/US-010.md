# US-010 — Global error handler + structured logging with correlation ID

_Generated by this story's proving commands · 2026-08-08 · PASS_

## 1. Correlation ID + global error handler are wired for every request, not one route

```console
$ grep -n 'requestId\|onError' apps/worker/src/index.ts
3:import { requestId } from 'hono/request-id';
5:import { CORRELATION_ID_HEADER, onError, type ErrorVariables } from './middleware/errors';
13:app.use('*', requestId({ headerName: CORRELATION_ID_HEADER }));
14:app.onError(onError);
```

## 2. Unhandled errors return the standard envelope with a correlation ID (never a raw stack trace), propagate a caller-supplied ID, and log route/correlation ID/user ID

```console
$ npm test --workspace apps/worker -- src/middleware/errors.test.ts --reporter=verbose

> @alvus-ai/worker@0.0.0 test
> vitest run src/middleware/errors.test.ts --reporter=verbose


 RUN  v4.1.10 /home/runner/work/Alvus-AI/Alvus-AI/apps/worker

 ✓ src/middleware/errors.test.ts > global error handler > returns the standard error envelope with a generated correlation ID, never a raw stack trace 14ms
 ✓ src/middleware/errors.test.ts > global error handler > propagates an incoming correlation ID instead of generating a new one 1ms
 ✓ src/middleware/errors.test.ts > global error handler > logs route, correlation ID, and user ID (when authenticated) for debugging without reproduction 3ms
 ✓ src/middleware/errors.test.ts > global error handler > logs the matched route pattern, not the resolved path, so param values are never embedded in the log line 1ms
 ✓ src/middleware/errors.test.ts > global error handler > logs userId as null when the request is unauthenticated 1ms

 Test Files  1 passed (1)
      Tests  5 passed (5)
   Start at  05:34:12
   Duration  698ms (transform 95ms, setup 0ms, import 170ms, tests 22ms, environment 0ms)
```

## 3. A live request against a running Worker: a caller-supplied correlation ID is echoed back on the response

```console
$ ./scripts/demo-us-010-server.sh
--- GET /api/health with a caller-supplied X-Correlation-Id ---
HTTP/1.1 200 OK
Content-Length: 25
Content-Type: application/json
X-Correlation-Id: demo-correlation-abc123

{"status":"ok","db":"ok"}
```
