# US-003 — Seed the tier_limits catalog and dev/CI fixture users

_Generated by this story's proving commands · 2026-08-06 · PASS_

## 1. Boot the local Supabase stack (dev/CI parity) and apply the baseline schema

```console
$ npx supabase start > /dev/null 2>&1; npx drizzle-kit migrate
No config path provided, using default 'drizzle.config.ts'
Reading config file '/home/runner/work/Alvus-AI/Alvus-AI/drizzle.config.ts'
Using 'postgres' driver for database querying
[⣷] applying migrations...{
  severity_local: 'NOTICE',
  severity: 'NOTICE',
  code: '42P06',
  message: 'schema "drizzle" already exists, skipping',
  file: 'schemacmds.c',
  line: '132',
  routine: 'CreateSchemaCommand'
}
{
  severity_local: 'NOTICE',
  severity: 'NOTICE',
  code: '42P07',
  message: 'relation "__drizzle_migrations" already exists, skipping',
  file: 'parse_utilcmd.c',
  line: '207',
  routine: 'transformCreateStmt'
}
[2K[1G[✓] migrations applied successfully!
```

## 2. Seed the tier_limits catalog and fixture users

```console
$ npm run db:seed

> alvus-ai@0.0.0 db:seed
> tsx db/seed.ts

Seeded 6 tier_limits rows.
Seeded fixture user "waitlist-pending" (waitlist-pending@example.test, status=pending, role=member).
Seeded fixture user "waitlist-approved" (waitlist-approved@example.test, status=approved, role=member).
Seeded fixture user "admin" (admin@example.test, status=approved, role=admin).
```

## 3. Re-run the seed to confirm it is idempotent (no duplicate-key errors, same row counts)

```console
$ npm run db:seed

> alvus-ai@0.0.0 db:seed
> tsx db/seed.ts

Seeded 6 tier_limits rows.
Seeded fixture user "waitlist-pending" (waitlist-pending@example.test, status=pending, role=member).
Seeded fixture user "waitlist-approved" (waitlist-approved@example.test, status=approved, role=member).
Seeded fixture user "admin" (admin@example.test, status=approved, role=admin).
```

## 4. Confirm the v1 tier_limits catalog (free 5/3, plus 60/30, pro 250/120)

```console
$ psql $DATABASE_URL -c 'select tier, action_type, monthly_limit from tier_limits order by tier, action_type;'
 tier |   action_type   | monthly_limit 
------+-----------------+---------------
 free | feedback_pass   |             3
 free | source_analysis |             5
 plus | feedback_pass   |            30
 plus | source_analysis |            60
 pro  | feedback_pass   |           120
 pro  | source_analysis |           250
(6 rows)
```

## 5. Confirm the three fixture users exist with correct status/role and a real backing auth.users row

```console
$ psql $DATABASE_URL -c "select u.email, u.status, u.role, (a.id is not null) as has_auth_user from users u join auth.users a on a.id = u.id order by u.email;"
             email              |  status  |  role  | has_auth_user 
--------------------------------+----------+--------+---------------
 admin@example.test             | approved | admin  | t
 waitlist-approved@example.test | approved | member | t
 waitlist-pending@example.test  | pending  | member | t
(3 rows)
```
