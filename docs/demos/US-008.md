# US-008 — Bootstrap first admin account

_Generated by this story's proving commands · 2026-08-07 · PASS_

## 1. Create a normal Supabase Auth account for the operator -- signing up never requires an admin to exist

```console
$ curl -s -o /dev/null -w '%{http_code}' -X POST "http://127.0.0.1:54321/auth/v1/signup" -H "apikey: sb_publishable_ACJWlzQHlZjBrEguHvfOxg_3BJgxAaH" -H 'Content-Type: application/json' -d "{\"email\":\"bootstrap-demo@example.test\",\"password\":\"Demo-Passw0rd!\"}"
200
```

## 2. Confirm no users/waitlist_signups row exists yet for that account (this script creates them if missing)

```console
$ psql "$DATABASE_URL" -c "select email, role, status from public.users where email = 'bootstrap-demo@example.test';"
 email | role | status 
-------+------+--------
(0 rows)
```

## 3. Promote it to admin -- a CLI script run directly against DATABASE_URL, never an HTTP endpoint

```console
$ npm run db:bootstrap-admin -- bootstrap-demo@example.test

> alvus-ai@0.0.0 db:bootstrap-admin
> tsx db/bootstrap-admin.ts bootstrap-demo@example.test

Promoted bootstrap-demo@example.test (352dffa0-36d7-4c17-836c-d7d324b1591c) to role=admin, status=approved.
```

## 4. Confirm users.role=admin, users.status=approved, and waitlist_signups is now approved too

```console
$ psql "$DATABASE_URL" -c "select email, role, status from public.users where email = 'bootstrap-demo@example.test';" -c "select email, status from public.waitlist_signups where email = 'bootstrap-demo@example.test';"
            email            | role  |  status  
-----------------------------+-------+----------
 bootstrap-demo@example.test | admin | approved
(1 row)

            email            |  status  
-----------------------------+----------
 bootstrap-demo@example.test | approved
(1 row)
```

## 5. Confirm re-running is idempotent (no duplicate-key errors, same result)

```console
$ npm run db:bootstrap-admin -- bootstrap-demo@example.test

> alvus-ai@0.0.0 db:bootstrap-admin
> tsx db/bootstrap-admin.ts bootstrap-demo@example.test

Promoted bootstrap-demo@example.test (352dffa0-36d7-4c17-836c-d7d324b1591c) to role=admin, status=approved.
```
