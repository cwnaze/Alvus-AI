# US-034 — docs/data-model.md documents the three rate-limit tables

_Generated by this story's proving commands · 2026-09-19 · PASS_

## 1. ER diagram references all three tables added by US-027

```console
$ grep -n 'share_link_lookups\|auth_rate_limit_attempts\|ai_rate_limit_attempts' docs/data-model.md | head -6
16:  users ||--o{ ai_rate_limit_attempts : "rate-limits"
26:  share_link_lookups
27:  auth_rate_limit_attempts
255:## `share_link_lookups`
269:## `auth_rate_limit_attempts`
285:## `ai_rate_limit_attempts`
```

## 2. share_link_lookups' per-table section lists the same fields as its Drizzle schema

```console
$ awk '/## `share_link_lookups`/,/^## `auth_rate_limit_attempts`/' docs/data-model.md
## `share_link_lookups`
Append-only log backing the public share-link endpoint's rate limit (US-027; not FK'd to
`share_links` — keyed by requester IP rather than a specific link since the endpoint is
deliberately unauthenticated). Same sum-at-read-time shape as `suggestion_requests`.
| Field | Type | Notes |
|---|---|---|
| `id` | uuid PK | |
| `ip_address` | text, not null | |
| `created_at` | timestamptz | |

Indexes: composite `(ip_address, created_at)` for the window-count query. Sensitive:
`ip_address` (PII-adjacent) — RLS enabled with zero policies and no GRANT, so
anon/authenticated are denied outright; readable only by the service-role connection.

## `auth_rate_limit_attempts`
```

## 3. auth_rate_limit_attempts' per-table section lists the same fields as its Drizzle schema

```console
$ awk '/## `auth_rate_limit_attempts`/,/^## `ai_rate_limit_attempts`/' docs/data-model.md
## `auth_rate_limit_attempts`
Append-only log backing the public auth endpoints' (signup/login/password-reset) per-IP
rate limit (US-027) — not owned by any user, since these endpoints are unauthenticated
by definition. `endpoint` keeps each route's window independent so a burst of login
attempts can't also lock the same IP out of signup.
| Field | Type | Notes |
|---|---|---|
| `id` | uuid PK | |
| `ip_address` | text, not null | |
| `endpoint` | text, not null | |
| `created_at` | timestamptz | |

Indexes: composite `(ip_address, endpoint, created_at)` for the window-count query.
Sensitive: `ip_address` (PII-adjacent) — RLS enabled with zero policies and no GRANT, so
anon/authenticated are denied outright; readable only by the service-role connection.

## `ai_rate_limit_attempts`
```

## 4. ai_rate_limit_attempts' per-table section lists the same fields as its Drizzle schema

```console
$ awk '/## `ai_rate_limit_attempts`/,/^## Migration strategy/' docs/data-model.md
## `ai_rate_limit_attempts`
Append-only log backing the AI-metered endpoints' (analyze, feedback) per-user rate
limit (US-027) — a burst guard layered in addition to the tier-quota check in
`usage_events`/`lib/metering`, same sum-at-read-time shape as `suggestion_requests`.
`action_type` mirrors `usage_events`' discriminator so the two action types never share
a window.
| Field | Type | Notes |
|---|---|---|
| `id` | uuid PK | |
| `user_id` | uuid, FK → `users.id` ON DELETE CASCADE | |
| `action_type` | text | enum: `source_analysis`\|`feedback_pass` |
| `created_at` | timestamptz | |

Indexes: composite `(user_id, action_type, created_at)` for the window-count query.
Sensitive: `user_id` ties usage bursts to a specific person — self-read only, RLS
`select` policy `user_id = auth.uid()` granted to `authenticated`; writes restricted to
the service-role connection.

## Migration strategy
```

## 5. Sensitivity summary table classifies each table's ip_address / user_id column

```console
$ grep -n 'share_link_lookups\|auth_rate_limit_attempts\|ai_rate_limit_attempts' docs/data-model.md | grep '|'
16:  users ||--o{ ai_rate_limit_attempts : "rate-limits"
336:| `share_link_lookups` | `ip_address` | PII-adjacent; no anon/authenticated read |
337:| `auth_rate_limit_attempts` | `ip_address` | PII-adjacent; no anon/authenticated read |
338:| `ai_rate_limit_attempts` | `user_id` | ties usage bursts to a person; self-read only |
```

## 6. field lists mirror apps/worker/src/lib/db/schema/ (ip_address, endpoint, action_type, user_id columns match)

```console
$ grep -n 'ipAddress\|endpoint\|actionType\|userId' apps/worker/src/lib/db/schema/share-link-lookups.ts apps/worker/src/lib/db/schema/auth-rate-limit-attempts.ts apps/worker/src/lib/db/schema/ai-rate-limit-attempts.ts
apps/worker/src/lib/db/schema/share-link-lookups.ts:3:// Append-only log backing the public share-link endpoint's rate limit
apps/worker/src/lib/db/schema/share-link-lookups.ts:7:// this endpoint is deliberately unauthenticated.
apps/worker/src/lib/db/schema/share-link-lookups.ts:12:    ipAddress: text('ip_address').notNull(),
apps/worker/src/lib/db/schema/share-link-lookups.ts:15:  (table) => [index('share_link_lookups_ip_created_at_idx').on(table.ipAddress, table.createdAt)],
apps/worker/src/lib/db/schema/auth-rate-limit-attempts.ts:3:// Append-only log backing the public auth endpoints' per-IP rate limit
apps/worker/src/lib/db/schema/auth-rate-limit-attempts.ts:6:// keyed by requester IP since these endpoints are unauthenticated by
apps/worker/src/lib/db/schema/auth-rate-limit-attempts.ts:7:// definition. `endpoint` keeps each route's window independent so a burst of
apps/worker/src/lib/db/schema/auth-rate-limit-attempts.ts:13:    ipAddress: text('ip_address').notNull(),
apps/worker/src/lib/db/schema/auth-rate-limit-attempts.ts:14:    endpoint: text('endpoint').notNull(),
apps/worker/src/lib/db/schema/auth-rate-limit-attempts.ts:17:  (table) => [index('auth_rate_limit_attempts_ip_endpoint_created_at_idx').on(table.ipAddress, table.endpoint, table.createdAt)],
apps/worker/src/lib/db/schema/ai-rate-limit-attempts.ts:4:// Append-only log backing the AI-metered endpoints' (analyze, feedback) per-user
apps/worker/src/lib/db/schema/ai-rate-limit-attempts.ts:7:// `actionType` mirrors usage_events' discriminator so the two action types
apps/worker/src/lib/db/schema/ai-rate-limit-attempts.ts:13:    userId: uuid('user_id')
apps/worker/src/lib/db/schema/ai-rate-limit-attempts.ts:16:    actionType: text('action_type').notNull(),
apps/worker/src/lib/db/schema/ai-rate-limit-attempts.ts:19:  (table) => [index('ai_rate_limit_attempts_user_action_created_at_idx').on(table.userId, table.actionType, table.createdAt)],
```
