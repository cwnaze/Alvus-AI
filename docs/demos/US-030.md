# US-030 — README and docs finalized

_Generated by this story's proving commands · 2026-08-31 · PASS_

## 1. README documents setup (install/serve.dev from pipeline.json), running tests, and deploying

```console
$ grep -n '^## ' README.md
7:## Documentation
16:## Development
60:## Deploying
112:## Tests and demos
125:## Build pipeline
```

## 2. The Deploying section matches what CI actually runs -- the same workflow files

```console
$ grep -n 'deploy-preview.yml\|deploy.yml' README.md
62:CI runs the deploy pipeline (`.github/workflows/deploy-preview.yml`,
63:`.github/workflows/deploy.yml`) — this is not a manual step:
```

## 3. README links an architecture/decisions doc for anyone picking up the project later

```console
$ grep -n 'docs/tdd.md' README.md
9:- **Architecture & decisions**: `docs/tdd.md`
```

## 4. Backup/restore procedure is documented, flagged as a pre-scale operational runbook item

```console
$ grep -n 'operational runbook item to revisit before scaling past bootstrap budget' README.md
107:operational runbook item to revisit before scaling past bootstrap budget**: upgrade to
```

## 5. Every environment variable in .env.example has a one-line purpose note in docs/infra.md

```console
$ node scripts/check-env-docs.mjs
26 variables in .env.example, 26 documented in docs/infra.md
```
