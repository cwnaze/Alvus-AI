# US-002 — Provision Supabase (Postgres + Storage) and connect Drizzle to a migrated baseline schema

_Generated by this story's proving commands · 2026-08-06 · PASS_

## 1. Boot the local Supabase stack (dev/CI parity)

```console
$ npx supabase start 2>&1 | grep -oE '"(API|DB|STUDIO)_URL":"[^"]*"'
"DB_URL":"postgresql://postgres:postgres@127.0.0.1:54322/postgres"
"API_URL":"http://127.0.0.1:54321"
"STUDIO_URL":"http://127.0.0.1:54323"
```

## 2. Confirm the source-uploads Storage bucket is private (RLS-ready, no public read until owner-scoped policies land in US-014/US-017)

```console
$ psql postgresql://postgres:postgres@127.0.0.1:54322/postgres -c "select id, name, public from storage.buckets where id = 'source-uploads';"
       id       |      name      | public 
----------------+----------------+--------
 source-uploads | source-uploads | f
(1 row)
```

## 3. Apply the baseline schema via drizzle-kit

```console
$ npx drizzle-kit migrate
No config path provided, using default 'drizzle.config.ts'
Reading config file '/home/runner/work/Alvus-AI/Alvus-AI/drizzle.config.ts'
Using 'postgres' driver for database querying
[⣷] applying migrations...[2K[1G[✓] migrations applied successfully!
```

## 4. Confirm users, waitlist_signups, and tier_limits exist, including the users -> auth.users FK

```console
$ psql postgresql://postgres:postgres@127.0.0.1:54322/postgres -c '\d public.users' -c '\d public.waitlist_signups' -c '\d public.tier_limits'
                               Table "public.users"
    Column    |           Type           | Collation | Nullable |     Default     
--------------+--------------------------+-----------+----------+-----------------
 id           | uuid                     |           | not null | 
 email        | text                     |           | not null | 
 display_name | text                     |           |          | 
 status       | text                     |           | not null | 'pending'::text
 role         | text                     |           | not null | 'member'::text
 created_at   | timestamp with time zone |           | not null | now()
 updated_at   | timestamp with time zone |           | not null | now()
Indexes:
    "users_pkey" PRIMARY KEY, btree (id)
    "users_role_idx" btree (role)
Foreign-key constraints:
    "users_id_users_id_fk" FOREIGN KEY (id) REFERENCES auth.users(id) ON DELETE CASCADE
Referenced by:
    TABLE "waitlist_signups" CONSTRAINT "waitlist_signups_reviewed_by_users_id_fk" FOREIGN KEY (reviewed_by) REFERENCES users(id)
    TABLE "waitlist_signups" CONSTRAINT "waitlist_signups_user_id_users_id_fk" FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE

                          Table "public.waitlist_signups"
… 6 lines omitted …
 requested_at | timestamp with time zone |           |          | 
 reviewed_at  | timestamp with time zone |           |          | 
 reviewed_by  | uuid                     |           |          | 
 notes        | text                     |           |          | 
Indexes:
    "waitlist_signups_pkey" PRIMARY KEY, btree (id)
    "waitlist_signups_status_idx" btree (status)
    "waitlist_signups_user_id_idx" UNIQUE, btree (user_id)
Foreign-key constraints:
    "waitlist_signups_reviewed_by_users_id_fk" FOREIGN KEY (reviewed_by) REFERENCES users(id)
    "waitlist_signups_user_id_users_id_fk" FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE

                Table "public.tier_limits"
    Column     |  Type   | Collation | Nullable | Default 
---------------+---------+-----------+----------+---------
 tier          | text    |           | not null | 
 action_type   | text    |           | not null | 
 monthly_limit | integer |           |          | 
Indexes:
    "tier_limits_tier_action_type_pk" PRIMARY KEY, btree (tier, action_type)
```
