# US-033 — Security doc's Authorization section matches the app's actual enforcement model

_Generated by this story's proving commands · 2026-09-19 · PASS_

## 1. docs/security.md states the real model: postgres role over DATABASE_URL, authorization via loadOwnedProject/requireApproved/authenticate, not anon-key RLS

```console
$ grep -n 'postgres.*role\|loadOwnedProject\|requireApproved\|authenticate' docs/security.md
30:**Live enforcement path: the Worker connects as the `postgres` role over
36:- `authenticate` (`middleware/auth.ts`) verifies the bearer token against the
38:- `requireApproved` rejects any request from a signed-in account whose
43:- `loadOwnedProject` (`routes/projects.ts`) is the ownership check every
51:- Share-link resolution (`routes/shared.ts`) → requester is unauthenticated by
60:approved-status gate, and revokes INSERT/UPDATE/DELETE from `anon`/`authenticated`
67:DB without a matching `loadOwnedProject`-style check (or an equivalent explicit
69:connection bypasses RLS by using the `postgres` role.
129:  `loadOwnedProject` (or an equivalent explicit check) before touching data — see the
```

## 2. docs/security.md clarifies RLS + grants are a secondary, independently-tested backstop and links US-026's RLS integration suite

```console
$ grep -n 'secondary.*backstop\|tests/rls/rls.test.ts' docs/security.md
56:**RLS + grants are a secondary, independently-tested backstop, not the live
63:invariant is verified independently by `tests/rls/rls.test.ts` (US-026's RLS
```

## 3. docs/security.md's secret classification table lists SHARE_LINK_ENCRYPTION_KEY as App runtime, sensitive

```console
$ grep -n 'SHARE_LINK_ENCRYPTION_KEY.*App runtime (sensitive)' docs/security.md
85:| `SHARE_LINK_ENCRYPTION_KEY` | App runtime (sensitive) | Never | No |
```

## 4. docs/data-model.md's Migration strategy section notes RLS is hand-written SQL that is not the live enforcement path

```console
$ grep -n 'RLS is a secondary, independently-tested backstop, not the' docs/data-model.md
261:  security doc's job. RLS is a secondary, independently-tested backstop, not the
```
